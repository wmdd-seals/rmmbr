service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // removed all security rules. The security rules are managed in other way
      allow read, write: if true;
    }
  }
}
